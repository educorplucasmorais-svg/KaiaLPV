# --- Configura��o do Banco de Dados MySQL (Hostinger) ---

# 1. URL de Conex�o ao Hostinger
spring.datasource.url=jdbc:mysql://srv109.hstgr.io:3306/u318705478_cla?useSSL=true&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=u318705478_u31870547
spring.datasource.password=SiteClara2025

# --- 2. CONFIGURA��O CR�TICA DO HIKARI CP (SOLU��O DE TIMEOUT) ---

# Hikari ir� tentar obter uma nova conex�o antes de falhar (fail-fast)
spring.datasource.hikari.connection-timeout=20000

# Hikari ir� manter as conex�es abertas por 30 segundos (30000ms)
# Previne que o Hostinger feche a conex�o por inatividade
spring.datasource.hikari.idle-timeout=30000

# Hikari ir� reabrir a conex�o a cada 60 segundos (60000ms)
# Isso garante que a conex�o remota n�o morra (Communications link failure)
spring.datasource.hikari.max-lifetime=60000

# --- 3. CONFIGURA��O GERAL DO JPA ---

# Usaremos 'update' para criar/atualizar tabelas automaticamente
spring.jpa.hibernate.ddl-auto=update

# Dialeto (manter por clareza, mesmo que o Spring o infira)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# --- 4. CONFIGURA��O DO SERVIDOR WEB ---
server.port=8080